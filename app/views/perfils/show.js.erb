<% if @perfil.id==params[:id].to_i %>
    <% @user=User.find(current_user.id) %>
<% else %>
    <% @user=User.find(Perfil.find(params[:id]).id_usuario) %> 
<% end %>


$("#modal-window").find(".modal-content").html("<%= j (render 'modales/modal_permiso') %>");
$("#modal-window").modal();
