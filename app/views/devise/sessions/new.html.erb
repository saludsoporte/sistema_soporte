<div class="container login borde-reporte" >
  <h2>Iniciar Sesi&oacuten</h2>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="row">
      <div><%= f.label :login %></div>
      <div class="col" ><%= f.text_field :login, autofocus: true, class: "form-control" %></div>
    </div>
    <div class="row">     
      <div><%= f.label :password,raw("Contrase&ntildea") %></div>     
      <div class="col"><%= f.password_field :password, autocomplete: "current-password",class: "form-control" %></div>
    </div>
  
    <div class="field">

      
    </div>
  <br>
    <div class="actions" >
      <%= f.submit "Iniciar Sesión",class: "btn btn-primary btn-sm btnInicio" %>
      <%= link_to "Revisar folio", folios_path , class: :"btn btn-link btn btn-sm " %>
    </div>
  <% end %>  
  <%= render "devise/shared/links" %>
</div>

